# 夜行手記開發規範

本檔適用於整個儲存庫。先保留使用者的內容、品牌口吻與既有功能，再考慮技術調整。

## 人與 AI 的分工

- 人負責需求、內容口吻、取捨與最終發布核准。
- AI 負責探索程式、提出方案、實作、測試與第一輪安全審查。
- 重大功能先規劃；錯字、單純文案與明確的小型調整可直接處理。
- 沒有使用者的明確授權，不可發布正式網站、變更外部帳號設定或執行不可逆操作。

## 不可破壞的安全邊界

- 公開網站只能讀取 `published` 文章，不能回傳草稿、封存文章、作者信箱或 Google 識別資料。
- 編輯室只允許 `STUDIO_HOST`，並依序通過 Cloudflare Access、Google 驗證、帳號白名單與網站工作階段。
- 密鑰、Token、白名單信箱及收件信箱只能放在託管環境的 Secret，不得寫入原始碼、Git、建置產物、錯誤訊息或日誌。
- 所有資料庫輸入必須使用參數綁定；文章不得直接輸出未清理的 HTML。
- 寫入 API 必須驗證工作階段、同源請求與預期的 Content-Type。
- 圖片上傳必須限制大小與格式，檢查檔案特徵並重新編碼以移除中繼資料。
- 修改認證、資料庫、檔案上傳、CSP 或發布流程時，必須補上對應測試。

## 工作流程簡寫

下列文字是對話中的工作流程指令，不是終端機命令。詳細規則見 `docs/ai-workflows.md`。

- `/plan-feature`：只探索與產生計畫，在取得核准前不實作。
- `/security-review`：唯讀安全檢查；只有加上 `--fix` 或取得明確授權才修改。
- `/simplify`：在行為、版面與公開介面不變的前提下降低複雜度。
- `/commit-message`：根據 staged diff 建議提交訊息，不自行 stage、commit 或 push。

## 完成前檢查

至少執行型別檢查、正式建置、測試、安全套件掃描及 `git diff --check`。審查變更是否超出需求、是否碰到上述安全邊界，以及是否意外加入秘密或個資。
